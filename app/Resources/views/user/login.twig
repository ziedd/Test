{% extends "layout.twig" %}
{% import '_macros.twig' as macros %}

{% block content %}
    <h1>Authentification</h1>

    {% if error %}
        <div class="row">
            <div class="col-lg-12">
                <div class="alert alert-dismissable alert-danger">
                    {{ error.messageKey|trans(error.messageData) }}
                </div>
            </div>
        </div>
    {% endif %}
    
        <form class="form-login" action="{{ path('security_login_check') }}" method="post">
            <fieldset>
                {{ macros.form_field('text', '_username', 'Email', last_username, 'test@leboncoin.com') }}
                {{ macros.form_field('password', '_password', 'Mot de passe', '', '') }}
                <div class="form-group">
                    <button type="submit" class="btn">Valider</button>
                    <a id="reg-btn" href="{{ path('user_register') }}">Enregister</a>

                </div>
            </fieldset>
        </form>

{% endblock %}

{% extends "layout.twig" %}
{% import '_macros.twig' as macros %}

{% block content %}
    <h1>Bienvenue !</h1>

    {% if errors is defined %}
        {{ macros.print_errors(errors) }}
    {% endif %}

        <form class="form-register" action="{{ path('user_register') }}" method="post">
            <fieldset>
                {{ macros.form_field('email', 'email', 'Email', user.email, 'test@leboncoin.com') }}
                {{ macros.form_field('text', 'username', 'Username', user.username, 'Username') }}
                {{ macros.form_field('password', 'plainPassword', 'Password', '', '') }}
                <div class="form-group">
                    <button type="submit" class="btn">Enregistrer</button>
                    <a class="cancel-link" href="{{ path('liste_contact') }}">Annuler</a>
                </div>
            </fieldset>
        </form>
{% endblock %}
